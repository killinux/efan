# mysql version database setting
jdbc.driver=com.mysql.jdbc.Driver
jdbc.url=${jdbc.url}
jdbc.username=${jdbc.username}
jdbc.password=${jdbc.password}

# dbcp settings
dbcp.maxIdle=${dbcp.maxIdle}
dbcp.maxActive=${dbcp.maxActive}

# 上传图片路径
img.file.path=${img.file.path}
img.url.path=${img.url.path}
img.cdn.path=${img.cdn.path}
meedo.img.dns=${meedo.img.dns}

# 上传apk路径
apk.file.path=${apk.file.path}
apk.url.path=${apk.url.path}
apk.cdn.path=${apk.cdn.path}
meedo.apk.dns=${meedo.apk.dns}